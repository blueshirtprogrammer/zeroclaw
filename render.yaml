# ZeroClaw Render Configuration
# Note: For Docker-based web services on Render, use the dashboard.
# This file is a reference only - do NOT use "render blueprints apply" with Docker

# To deploy:
# 1. Go to https://render.com
# 2. New → Web Service
# 3. Connect: blueshirtprogrammer/zeroclaw
# 4. Environment: Docker
# 5. Docker Image: agent0ai/agent-zero:latest
# 6. Add env vars in dashboard

services:
  - name: zeroclaw
    type: web
    region: oregon
    plan: free
    # Docker image is set in Render dashboard, not here
    envVars:
      - key: ZC_BRAND_NAME
        value: ZeroClaw
